# Fact Store - Avro

This subproject aims to ease the integration of FactStore with Avro serialized events.
FactStore Avro is a layer built on top of the core layer that can act as a bridge between the client code and FactStore. 